
# This will list all the wifi profiles
netsh wlan show profiles

# Then pick the profile name you want to know the password to
# for example if the profile name is "hammer" then run
# `netsh wlan show profile name="hammer" key=clear`

